class Graph{
    constructor(routerList){
        this.routerList = routerList;
        this.routerIDs = Object.keys(routerList);
        this.totRouterTable = {};
    }

    dijkstra(router){
        // adds a routing table for the input router to this.routerTable
    }    

}